*************** move_on:
*** 2104,2110 ****
  
  	if(!shkp->mgold) {
  		char c, qbuf[BUFSZ];
! 		long tmpcr = (ltmp + cltmp) * 2L;
  
  		if (sell_response != 'n') {
  		    pline("%s cannot pay you at present.", Monnam(shkp));
--- 2223,2229 ----
  
  	if(!shkp->mgold) {
  		char c, qbuf[BUFSZ];
! 		long tmpcr = (ltmp + cltmp)/* * 2L*/;
  
  		if (sell_response != 'n') {
  		    pline("%s cannot pay you at present.", Monnam(shkp));
